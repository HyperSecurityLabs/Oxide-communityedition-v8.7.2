#!/bin/bash
# OXIDE CE — Terminal Launcher
# Author: KhaninKali @ HyperSecurity

export LD_LIBRARY_PATH="/usr/lib:/usr/lib/oxide:$LD_LIBRARY_PATH"

if [ $# -eq 0 ]; then
    /usr/bin/oxide --help 2>&1
    echo ""
    echo "  ──────────────────────────────────────────────"
    echo ""
    exec bash
else
    /usr/bin/oxide "$@"
fi
